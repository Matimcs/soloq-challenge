# Dependencias
node_modules/
backend/node_modules/
overlay/node_modules/

# Secretos y config local  ⚠ NUNCA subir
.env
backend/.env
overlay/settings.json

# Caché de la Riot API (PUUIDs cifrados por key)
cache/

# Logs / temporales
*.log
npm-debug.log*

# Datos generados por el runner (el server online los recibe por /api/ingest)
players.json
players.js

# Roster extra (lo genera el server desde la DB)
roster-extra.json
